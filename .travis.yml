language: python

script:
  - pip install virtualenv
  - make devbuild

matrix:
  include:
  - os: linux
    dist: xenial
    sudo: required
    compiler: gcc
    python: 3.7
    addons:
      apt:
        sources: 
          - ubuntu-toolchain-r-test
        packages:
          - g++-7
    env: CC=g++-7
